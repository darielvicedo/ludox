{{ form_start(form, {'attr': {'class': 'row g-3', 'data-action': 'asset#new'}}) }}
<div class="col-12">
    {{ form_label(form.ci, 'CI', {'label_attr': {'class': 'form-label'}}) }}
    {{ form_widget(form.ci, {'attr': {'class': 'form-control'}}) }}
</div>

<div class="col-12">
    {{ form_label(form.name, 'Nombre', {'label_attr': {'class': 'form-label'}}) }}
    {{ form_widget(form.name, {'attr': {'class': 'form-control'}}) }}
</div>

<div class="col-12 text-end">
    <button type="submit" class="btn btn-sm btn-{{ button_class|default('success') }} text-white">
        {{ button_label|default('Crear') }}
    </button>
</div>
{{ form_end(form) }}