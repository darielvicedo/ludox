<nav class="navbar navbar-expand-lg bg-body-tertiary navbar-light bg-white py-0">
    <div class="container-fluid">
        <a
                class="navbar-brand border-end pe-4 me-0 d-flex justify-content-start align-items-center"
                href="{{ path('app') }}"
        >
            <img
                    src="{{ asset('build/img/logo.png') }}"
                    alt="30"
                    height="30"
                    class="d-inline-block align-text-bottom rounded me-2"
            >
            <span class="fs-suez text-danger">LudoX</span>
        </a>
        <button
                class="navbar-toggler"
                type="button"
                data-bs-toggle="collapse"
                data-bs-target="#appNavbar"
                aria-controls="appNavbar"
                aria-expanded="false"
                aria-label="Toggle navigation"
        >
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="appNavbar">
            <ul class="navbar-nav align-items-center me-3">
                <li class="nav-item border-end px-2">
                    <a href="#" class="nav-link">
                        <i class="bi-people-fill"></i>
                    </a>
                </li>
                <li class="nav-item border-end px-2">
                    <a href="{{ path('admin') }}" class="nav-link">
                        <i class="bi-gear-fill"></i>
                    </a>
                </li>
            </ul>
            {{ render(controller('App\\Controller\\AppController::staticNavbarNetValue')) }}

            <ul class="navbar-nav ms-auto align-items-center">
                <li class="nav-item me-3">
                    <a href="#" class="nav-link p-0">{{ app.user.username }}</a>
                </li>
                <li class="nav-item">
                    <a
                            href="{{ path('app_logout') }}"
                            class="btn btn-sm btn-danger"
                            title="logout"
                            data-bs-toggle="tooltip"
                            data-bs-placement="top"
                    >
                        <i class="bi bi-power"></i>
                    </a>
                </li>
            </ul>
        </div>
    </div>
</nav>